---
name: btc-monthly-report
description: 生成或更新加密资产月度专业调研报告（Markdown + HTML + PDF），目前覆盖 BTC 与 SOL。当 _dalin 说"生成 X 月版报告""更新 BTC/SOL 报告""出一份新的调研""按上次那套做一份"时使用。也用于修改已有月份的报告后重新出 HTML/PDF。
---

# 加密资产月度专业调研报告

支持多资产。构建时用 `--asset` 指定，资产档案登记在 `build.py` 的 `PROFILES` 字典里。

## 资产间的框架差异（SOL 接入时实测得出）

约六成结构可复用，四成必须换：

| BTC 章节 | SOL 对应 |
|---|---|
| 减半周期 | ❌ → **通胀递减与销毁机制**，且 SOL 的通胀**可被治理投票修改** |
| 算力 / 矿工经济 | ❌ PoS 无挖矿 → **质押率与验证者经济** |
| 数字黄金叙事 | ❌ → **高性能应用执行层** |
| AI 多维影响 | ⚠️ 降级并入生态数据 |
| 上市公司储备 | ✅ 保留，SOL 渗透率约为 BTC 一半 |
| — | ➕ **网络稳定性与宕机史** |
| — | ➕ **生态数据**（TVL / DEX / 稳定币 / RWA） |
| — | ➕ **治理投票** |
| — | ➕ **与 ETH 的 L1 竞争** |

**最根本的差异**：BTC 是货币资产，估值锚是稀缺性；SOL 是**生产性资产**，估值锚应是**网络收入**。
因此 SOL 的情景推导用「假设年化收入 × 假设收入倍数」，而非 BTC 用的「已实现价格 × MVRV」。
接入新资产时先问：**它靠什么产生价值？** 答案决定第 9 章的推导方法。

## ADA 接入实测结论（2026-08 首期已完成）

预研阶段的假设**多数被推翻**，记录如下供后续资产参考：

| 预研假设 | 实测结果 |
|------|------|
| 「市值高、链上使用率低」 | ✅ **成立且比预想极端**：市值 $80.4 亿，L1 月费 $38,275，8 月交易 64.6 万笔（SOL 的 1/8000） |
| 「估值问题与 SOL 相反，需要第三种框架」 | ✅ **成立**。BTC 靠稀缺性、SOL 靠网络收入，**ADA 两条都够不着**——收入法失效**不是因为收入低**，而是**分母在噪声量级**（两个口径差 2.7 倍）。改用相对市值 + 情景概率，**但必须声明其分辨率不足以判断「低估/高估」** |
| 「治理已进入 Voltaire 阶段」 | ✅ 成立，且**治理是本期核心章**（替代 BTC 版的「上市公司储备」主位——ADA 企业储备近乎为零，不足以支撑一章） |
| 「Hydra 是主要二层方案」 | ⚠️ 部分。Leios（L1 扩容）才是当前主线，Hydra 与 Midgard rollup 是配套 |
| 「总量上限 450 亿，货币政策固定」 | ✅ 成立。无减半、无销毁，每 epoch 释放 reserves 的 0.3%，通胀 1.47% |

**ADA 版相对 SOL 版的章节改动约三成**：

| SOL 章节 | ADA 对应 |
|---|---|
| 通胀销毁机制 | ⚠️ 改为**储备衰减与国库**（ADA 无销毁机制） |
| 网络稳定性与宕机史 | ❌ 不适用（ADA 无重大共识事故）→ 并入起源章的技术取舍 |
| 治理投票 | ⬆️ **升为独立核心章**（第 5 章），因为它是 ADA 当期的第一变量 |
| 上市公司储备 | ⬇️ 降级并入「机构通道与持有者结构」（渗透率近乎为零） |
| — | ➕ **基金会自身资产负债表**（最了解这条链的机构如何配置自己的钱，是有力的旁证） |

## 接入新资产的步骤

## 接入新资产的步骤

1. 在 `build.py` 的 `PROFILES` 加一条（symbol / name / short / accent / badge）
2. 建 `<asset>/professional/charts/` 目录
3. 按上表调整章节框架
4. 建该资产独立的 `thesis_scorecard.md`（**不与其他资产混记**）

> SOL 接入时暴露的硬编码共 **5 处**（标题、顶栏、侧栏、hero、徽章）+ 主题色，比原先估计的 2 处多。
> 现已全部集中到 `PROFILES`，接入第三个资产应该只需改那一处。

# BTC 月度专业调研报告（以下流程通用）

产出一套四件产物，放在 `btc/professional/`：

| 文件 | 说明 |
|------|------|
| `btc_research_report_<YYYYMM>.md` | 完整报告，11 章 |
| `btc_questions_summary_<YYYYMM>.md` | 简明速查，10-11 节 |
| `btc_research_report_<YYYYMM>.html` | 网页版（脚本生成，勿手写） |
| `btc_research_report_<YYYYMM>.pdf` | PDF（Chrome 无头渲染） |
| `charts/*-<YYYYMM>.svg` | 三张图表 |
| `thesis_scorecard.md` | **跨期滚动**，不带月份后缀，每期追加；**每个资产各一份，不混记** |

通俗版是**另一条线**，放 `btc/general/`，不在本技能范围（且被 gitignore 排除）。

---

## 第 1 步：抓数据

用 WebSearch 覆盖这 7 类。**不要凭记忆写数字**——每个月都变，写错就是硬伤。

1. **价格与市场**：现价、距 ATH 回撤、市值、关键支撑阻力、恐惧贪婪指数
2. **ETF 资金流**：当月净流入/出、YTD 累计、总 AUM、IBIT / FBTC 分项
3. **宏观**：美联储主席立场与利率路径、CPI / PCE、10 年美债、DXY、油价、黄金、标普
4. **链上**：MVRV、MVRV Z-Score、STH-MVRV、SOPR、LTH-SOPR、已实现价格、LTH 已实现价格、交易所余额、Puell Multiple
5. **上市公司储备**：总持仓、Strategy / Metaplanet / MARA 等头部动向、**有没有人在卖**、mNAV
6. **监管**：CLARITY 法案进度与通过概率、SBR / ARMA、各国政策
7. **AI 相关**：矿工转 AI/HPC、AI Agent 支付（x402 / L402 / 闪电网络）、量子威胁、AI 诈骗

搜索时把年月写进 query（如 `Bitcoin ETF flows August 2026`），否则容易搜到旧数据。

### ⚠️ 硬规则：凡有公开 API 的指标，一律不用搜索摘要（ADA 首期实测得出）

ADA 2026-08 期用一手 API 推翻了**五处**广泛流传的媒体数字，**没有一处是靠「多找几篇报道」能发现的**：

| 媒体口径 | 一手数据 | 差异性质 |
|---|---|---|
| IOG 研究预算「遭 86.72% 反对否决」 | **74.94% 赞成通过，已拨款** | **结论完全相反** |
| TVL 高点「2025-08 的 $4.372 亿」 | 2024-12-07 的 **$7.21 亿** | 回撤 –87.6% vs –91.0% |
| 质押率「63%–70%」 | **56.4%–57.0%** | 疑似沿用 2021–23 年数据 |
| 国库「超过 10 亿美元」 | **$2.88 亿** | 对应旧价格 |
| 基金会「BTC 持仓上升 25%」 | 占比升至 25.5%，**持仓降 37.8%** | **方向相反** |

**两条必须遵守的规则**：

1. **多家媒体报道一致 ≠ 事实。** 它们很可能引用同一时点的同一份快照。
2. **治理类数据必须查最终状态**（`ratified_epoch` / `enacted_epoch`），**不得使用任何时点的得票率报道**。
   加密治理投票期跨多个 epoch，**大量票在后期涌入**，中途快照可以与最终结果完全相反。

**已验证可用的免费一手 API（无需 key）**：

```bash
# Cardano 链上：epoch 数据、治理动议最终状态、供应量、质押池
curl "https://api.koios.rest/api/v1/epoch_info?_epoch_no=653"
curl "https://api.koios.rest/api/v1/proposal_voting_summary?_proposal_id=<gov_action...>"
curl -G "https://api.koios.rest/api/v1/proposal_list" \
  --data-urlencode "proposal_type=eq.TreasuryWithdrawals" \
  --data-urlencode "select=proposal_id,proposed_epoch,enacted_epoch,expired_epoch,title:meta_json->body->>title"

# TVL / 费用 / 稳定币（defillama.com 网页被网络策略拦截，但 API 可用）
curl "https://api.llama.fi/v2/historicalChainTvl/Cardano"
curl "https://api.llama.fi/summary/fees/cardano?dataType=dailyFees"   # 只有 L1 链费
curl "https://api.llama.fi/overview/fees/cardano"                     # 生态合计（含应用层）
```

### ⚠️ 费用口径：L1 链费 ≠ 生态合计费用

DefiLlama 的 `summary/fees/<chain>` 返回的是 **L1 协议层费用**；`overview/fees/<chain>` 才是**含应用层的生态合计**。
ADA 首期实测：L1 30 天 $38,275，生态合计 $353,264，**相差 9.2 倍**。

**两者用途不同，不能混用**：

- **L1 费** → 衡量**代币的价值捕获**（这部分才流向持币人的奖励池与国库）
- **生态合计费** → 衡量**生态活跃度**（归各应用协议，不流向持币人）

把 L1 费称作「全网手续费」并用它论证「生态没人用」，是**口径偷换**——ADA 初稿就是这么写的。
这与 SOL 首期的教训同源：**网络毛收入 ≠ 持币人现金流。**

### ⚠️ 识别风险 ≠ 处理风险

ADA 初稿在附录里**自己**把「86.72%」标注为「仅搜索摘要、未打开一手投票记录」，
在记分卡里**自己**写了「这是本期最重要却核实等级最低的数字，下期必须补一手核实」——
**然后就把它写进了核心结论。**

**不允许把已知的高风险项推迟到「下期核实」。** 凡链上可查的事实，写作阶段就必须查链上。

## 第 2 步：写完整报告（11 章）

框架固定，逐月只换数据和判断：

| 章 | 内容 | 逐月要更新什么 |
|---|------|--------------|
| 0 | 核心结论摘要（一页速览表） | 全部 |
| 1 | 起源与历史背景 | 基本不动 |
| 2 | 发展阶段划分（五阶段） | 只在阶段五追加当月大事 |
| 3 | 当前现状（资产属性 / 市场结构 / 链上 / 宏观 / 技术生态） | **全部重写** |
| 4 | 上市公司比特币储备 | **全部重写** |
| 5 | AI 科技的多维影响 | 更新进展 |
| 6 | 核心价值逻辑（多头 ★ 评分 / 空头 ★ 评分） | 调整强度评级 |
| 7 | 与其他资产比较（黄金 / 美股 / 美债 / ETH） | 更新表现数据 |
| 8 | 未来 1–5 年关键变量（催化剂日历） | **全部重写** |
| 9 | 三种情景推演 + 主观概率 | 调整概率并说明理由 |
| 10 | 投资与风险启示 | 更新周期判断 |
| 11 | 附录（阶段速查 / 减半对比 / 储备速查 / AI 速查 / 数据来源） | 更新数据行 |

写作要求（_dalin 的明确偏好）：

- **中文**。专有名词保留英文原文但要解释它是什么
- **不写成宣传文**。多空都要给，不确定的地方明说"未验证"
- **重点不是预测价格**，是理解历史、现状、逻辑、可能性
- 表格优先于长段落
- 报告开头的引用块里写明「本版更新要点」——列出相比上一版变了什么
- **给出价格区间必须同时给出推导方式**（见 202608 版 9.4 节的写法）。精确的数字如果没有方法论，就是给主观判断打包装
- **必须纳入非加密原生来源**。Cointelegraph / CoinDesk / KuCoin 这类媒体结构上偏多；固定引一节传统机构观点（Deutsche Bank、Citi、JPMorgan 等）配平
- 附录的来源表要标注**核实状态**：哪些是一手抓取，哪些只是搜索摘要，哪些没核实成
- 情景概率变动要说明原因（如"悲观概率上调，因 Strategy 开始抛售"）

## 第 2.5 步：出图表

每期至少三张，放 `btc/professional/charts/`，命名 `<主题>-<YYYYMM>.svg`，在 Markdown 里用相对路径引用：

```markdown
![2026 年 5–8 月 ETF 月度净流量](charts/etf-flows-202608.svg)
```

**为什么用独立 SVG 文件而不是内联**：GitHub 的 Markdown 渲染器会剥掉内联 `<svg>`，但能正常显示 `![](x.svg)` 引用的图片。独立文件同时满足 GitHub 预览、HTML 构建、PDF 渲染三处。

固定的三张：

| 图 | 内容 | 形式 |
|---|------|------|
| ETF 月度净流量 | 近 4 个月净流入/出 | 柱状图，零轴上下分色 |
| 持有者成本基准 vs 现价 | STH / LTH 成本、已实现价格 | 横向条形 |
| 三情景价格区间 | 概率 + 区间 | 横向区间条 |

**配色必须先跑校验器**，不要凭感觉：

```bash
D=/private/tmp/claude-501/bundled-skills/*/dataviz   # 路径每次可能不同，用 Skill 工具载入 dataviz 获取
node $D/scripts/validate_palette.js "#2a78d6,#e34948" --mode light
```

已验证可用的组合（浅色底）：

- **双极（流入/流出）**：`#2a78d6` 蓝 / `#e34948` 红 — CVD ΔE 21.6，全部 PASS
- **三分类（三情景）**：`#2a78d6` / `#eb6834` / `#1baf7a` — all-pairs 全 PASS
- 文字 `#1c1917`（主）/ `#57534e`（次），网格 `#e7e5e4`

> ⚠️ 红配青（`#e34948` + `#1baf7a`）在 deutan 色盲下 ΔE 仅 6.9，落在警戒带，**不要用**。

**画完必须实际渲染检查**（dataviz 规范第 7 步）——校验器只管颜色，不管排版：

```bash
# 建一个临时预览页放在 btc/professional/ 下（相对路径才解析得到 charts/）
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu \
  --screenshot=/tmp/charts.png --window-size=720,1030 --hide-scrollbars \
  "file://$(pwd)/btc/professional/_preview.html"
```

然后用 Read 工具看图。**上次就是靠这一步抓到两个问题**：坐标轴标签与脚注重叠、窄条上的标签被裁切。检查完删掉临时预览页。

## 第 3 步：写简明总结

10-11 节，是完整报告的浓缩，给快速复习用。固定以「如果你只有 3 分钟，记住这 5 句话」收尾。

末尾注明配套文件：``> 详细论证请阅读 `btc_research_report_<YYYYMM>.md`。``

## 第 4 步：构建 HTML + PDF

**不要手写 HTML**，用脚本：

```bash
python3 .claude/skills/btc-monthly-report/build.py <YYYYMM>
```

脚本会自检并打印导航条数、锚点是否全部命中、表格容器数、有无重复锚点。**校验不过会退出非零**。

然后按脚本末尾打印的命令生成 PDF（Chrome 无头模式，会自动套用 `@media print` 样式隐藏侧边栏）。

## 第 4.5 步：更新论点记分卡

`btc/professional/thesis_scorecard.md` 是**跨期滚动**的，每期必做两件事：

1. **核对上期登记的论点**，填入结果与判定（✅ 正确 / ⚠️ 部分 / ❌ 错误）
2. **登记本期新论点**，每条必须写明**可证伪的检验条件**——不可证伪的论断不予登记

**检验条件的三条门槛**（第 1 条来自 SOL 首期，第 2–3 条来自 ADA 首期的推理审查）：

| # | 要求 | 反例 |
|---|------|------|
| 1 | **门槛必须够得着** | SOL 的「验证者回升至 1,100」，而已披露数列是 791→906 |
| 2 | **论点必须是预测，不是定义** | 「ADA 是期权型资产」是框架选择，无法被世界证伪，不予登记 |
| 3 | **证伪区间必须与论点区间同构** | 论点说「落在 $0.13–$0.31」，证伪条件却是「$0.10–$0.40 之外」——带宽放大三成，等于换了个更容易对的论点 |

> **写检验条件本身也会出错，且错法更隐蔽**——虚高的正确率不会自己暴露。ADA 首期登记的六条里有三条在审查中被判不合格并重写。

然后更新"累计战绩"表，并检查"反复出现的错误模式"一节是否需要新增条目。

> 这一步不能省。报告每月重写一遍很容易，但**不检查上月说对了没有，就只是每月一次的情绪复读**。
> 截至 2026-08，已检验的 8 个论点里只说对了 1 个——这个数字必须公开挂着。

## 第 4.8 步：四层审查（发布前必做，不可跳过）

**背景**：截至 2026-08，BTC 记分卡显示自查过的论点只有 1/8 说对；SOL 首期图表里出现过一个编造的数字，
靠人工渲染检查才偶然发现。**写稿和检查是同一个人做，盲区是同一个。**

```
写稿 → ① 脚本核查 → ② _bian 审推理 → ③ Codex 审领域 → ④ _dalin 抽查 → 发布
```

### 第 1 层：机械核查（脚本，必须先过）

```bash
python3 tools/verify_report.py <asset> <YYYYMM>
```

有 FAIL 就退出非零，**不得进入下一层**。它检查六项：

| 检查 | 抓什么 |
|---|---|
| **图表数字溯源** | 图里显示了正文没有的数字 —— **最重要，编造数据必然在此暴露** |
| 来源核实状态表 | 附录是否标明哪些一手抓取、哪些只是摘要 |
| 来源偏向 | 是否引用了传统机构配平加密原生媒体的看多倾向 |
| 价格区间方法论 | 给了区间是否附推导与弱点 |
| 记分卡登记 | 本期论点是否登记、是否写了可证伪条件 |
| 数据新鲜度 | 是否大量引用过期年份 |

> 实测有效：脚本上线当天就在**已发布的** BTC 8 月版里抓到两处——
> 图表显示的成本基准 $81,821/$59,506 正文里没有，以及图表标 +1.7 而正文写 +1.72。

### 第 2 层：`_bian` 审推理（全局 agent）

把成稿交给 `_bian`，它判推理不判事实。重点让它审：

- 三情景的概率赋值凭什么
- 估值倍数/增速假设的依据够不够
- 有没有把「结构性脆弱」写成「即将断裂」（这是记分卡里出现 3 次的惯犯模式）
- 结论有没有超出证据

### 第 3 层：Codex 审领域

```bash
codex exec --skip-git-repo-check '你是加密资产研究的领域怀疑者。读 <path> 这份报告，
只回答一个问题：它最可能错在哪？给三条，按严重程度排序，直接指出，不要复述内容。'
```

Codex 用的是 cc-switch 自定义 provider，`models_cache.json` 恒为空，
**`cc-suite:codex-preflight` 会误报不可用——直接用 `codex exec` 即可**。

### 第 4 层：_dalin 抽查

只需查 **3–5 个「错了会改变决策」的数字**。脚本末尾会列出大数清单供挑选。
例：SOL 那期是收入 –87%、质押率 64%、Upexi –96%。

### ⚠️ 修订传导清单（SOL 首期踩过的坑）

**审查改完正文后必须回扫，否则会制造比原错误更糟的自相矛盾。**

SOL 首期就是这样：Codex 的三处更正只改了正文，摘要、多空表、监控指标全留在旧版本上，
结果同一份文件里出现十余处互相打架——**而只读摘要的人拿到的恰恰是已被正文推翻的版本**。

改动任何一个正文结论后，强制回扫这五个位置：

```
正文结论改动 → ① 核心结论摘要表 → ② 6.1/6.2 多空评分（含星级）
             → ③ 第 8 章催化剂日历 → ④ 10.3 监控指标 / 10.5 长期认知
             → ⑤ thesis_scorecard.md 的检验条件
```

特别注意 ⑤：**改了正文的判断，检验条件往往就失效了。**
SOL 的 S8-2 原本用「全网中断」做证伪条件，而 3.4 修订后恰恰论证了 8/12 那类事件不算全网中断——
等于承诺这条论点无法被自己识别出的风险推翻。

**记分卡的全部价值来自「能判错」。** 检验条件要能被现实触发，
门槛设得够不着（如「验证者回升至 1,100」，而已披露数列是 791→906）等于自我豁免。

### 审查救不了的一类问题

**来源偏向修不了。** 数据大量来自生态内部来源（Helius、Solana Compass、Cointelegraph 等）时，
审推理不会发现，Codex 也未必。**唯一解法是抓数据阶段就主动找空方来源**——这是写作纪律，不是审查能补的。

## 第 5 步：提交

```bash
git add btc/ && git commit && git push
```

---

## 三个坑（已在脚本里根治，别自己绕回去）

### ① 中文标题的锚点是拼音 slug
Python markdown 把「4. 上市公司比特币储备」转成 `id="4-7"`、「5. AI 科技对 BTC 的多维影响」转成 `id="5-ai-btc"`。**凭空猜必然点不动导航。** 脚本改为从生成的 HTML 里自动抽取 h2 的真实 id，不再手工映射。

### ② 两份 Markdown 合并会撞锚点
完整报告和简明总结各自编号，都可能生成 `id="_1"`。合成一页后点第二个会跳到第一个。脚本给总结的所有标题 id 加 `s-` 前缀隔离，并在自检里检测重复。

### ③ 布局两件事
- **桌面端正文不能设窄的固定 max-width**。曾经设 900px，结果窗口拉大后右侧一大片空白。现用 `padding: clamp(40px,5vw,90px)` 流式铺满，只在超宽屏（>1660px）用 1400px 兜底
- **表格必须包 `.tbl` 滚动容器**。专业版表格设了 `min-width:520px`（5-7 列对比矩阵），不包容器在手机上会撑破版面

---

## 数据来源清单

链上 Glassnode / CryptoQuant · ETF 流量 Farside / CoinGlass / The Block · 上市公司储备 BitcoinTreasuries.com / CoinGlass · 宏观 CME FedWatch / 美联储 · 机构研究 Fidelity / Bernstein / CryptoQuant

## 分析框架来源

报告骨架来自 `anthropics/financial-services` 插件的四个 Skill，装在中央缓存（不在本项目内）：

- `market-researcher:sector-overview` — 行业全景、市场结构
- `equity-research:thesis-tracker` — 多空逻辑评分卡
- `equity-research:catalyst-calendar` — 催化剂日历
- `financial-analysis:competitive-analysis` — 资产横向对比矩阵

启用配置在 `.claude/settings.json`。新机器恢复：`claude plugin marketplace add anthropics/financial-services`
